# 本地运行数据

本目录定义客户端本地可写数据边界。新运行证据写入 `runs/`，该子目录由应用按 create-new 与原子更新规则管理并被 Git 忽略。

旧顶层 `.runs` 已按字节不变方式迁入 `legacy-runs/`：共 21 个 Run 目录、257 个文件、5,066,943 字节，迁移前后树 SHA-256 均为 `FE088BFC5302A1D98F1C0380A2A5CE94C2CD604BBA5278DE3A5BA1BD4EBEAC64`。历史文件正文保留原路径，不作为现行入口，也不由 `RunStore` 枚举。

运行数据不是源码、打包资源或发布证明。清理前应确认不再需要其中的审计证据；服务端状态不能替代本地证据，也不能提升部署、全技能覆盖或客户端兼容状态。
